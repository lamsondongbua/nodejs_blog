//1. Server side rendering (phía máy chủ)
// - Thường được sử dụng với các website cần SEO (mang tính search,blog, thương mại) (web báo chí)
// - Dấu hiệu nhận biết: chuột phải, view page source, nếu trả ra html thì là đúng
// - reload lại cả trang

//2. Client side rendering (phía người dùng)
// - Dấu hiện nhận biết: chuột phải, view page source, nếu trả về mã lộn xộn nhìn lú não thì là đúng
// - Thường được sử dụng với các web SPA (tương tác người dùng cao) hoặc liên quan tới thời gian thực
// - reload phần được chọn